# Claude API key. Powers both POST /rag/chat and /parcel-detail's rag_result
# (see permit_rag.py, consolidated from the teammates' Rag_permit/ project —
# keyword retrieval over permit_type_stats.csv + one Claude call). If unset,
# both endpoints return a mock answer instead of calling out.
ANTHROPIC_API_KEY=
