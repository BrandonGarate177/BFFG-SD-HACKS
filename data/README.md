# ML predictions — data handoff

Generated by the permit-timing pipeline. These are the only files the server and tile
build need; the training code lives outside this repo.

Two files. Both keyed on `apn` (10-digit string, no dashes, leading zeros preserved).

| File | Rows | Size | For |
|---|---|---|---|
| `predictions.parquet` | 1,575,020 | 42 MB | `GET /parcels/{apn}` detail lookup |
| `parcels_tile_attributes.parquet` | 393,755 | 6.7 MB | tippecanoe → `parcels.pmtiles` |

Same underlying data. The first is **long** (one row per parcel × archetype), the second is
**wide** (one row per parcel, predictions widened into columns) because a MapLibre filter
expression evaluates against a single feature's properties.

Coverage is the **City of San Diego only** — 393,755 parcels that fall inside a City base
zoning polygon, out of 1,088,722 county-wide. DSD permit data doesn't cover unincorporated
county, so predictions would be undefined there.

---

## `parcels_tile_attributes.parquet` — for the tile build

One row per parcel, 18 columns. Join to parcel geometry on `apn`.

| Column | Type | Notes |
|---|---|---|
| `apn` | string | join key |
| `zone` | string | City base zone, e.g. `RS-1-7`, `RM-1-1`, `CC-3-5` |
| `situs_community` | string | community name; **2% null** |
| `lot_sqft` | Int32 | from parcel polygon area |
| `existing_units` | Int32 | dwelling units already built |
| `delta_units` | Int32 | **unbuilt by-right capacity — the headline filter**; null in non-residential zones |
| `adu_eligible` | bool | |
| `sb9_eligible` | bool | |
| `in_coastal_overlay` | bool | 46,511 parcels |
| `coastal_deferred_certification` | bool | 2,367 parcels — see caveats |
| `pred_days_adu` | Int32 | median days to permit issuance |
| `pred_days_duplex` | Int32 | |
| `pred_days_3_4_unit` | Int32 | |
| `pred_days_5plus` | Int32 | |
| `prob_1yr_adu` | float32 | probability issued within 365 days |
| `prob_1yr_duplex` | float32 | |
| `prob_1yr_3_4_unit` | float32 | |
| `prob_1yr_5plus` | float32 | |

The active archetype selects which `pred_days_*` the filter expression reads.

**Not included, and you'll need it for two filters the architecture lists:** `tpa` (Transit
Priority Area) and Sustainable Development Area. Neither is computed — SDA is a *network
walking distance* along sidewalks from a major transit stop, not a radius buffer, so a
straight-line approximation would be materially wrong in canyon-fragmented neighborhoods.
If you want those filters, that's a separate network-analysis step.

---

## `predictions.parquet` — for the detail endpoint

One row per (`apn`, `archetype`). `archetype` ∈ `adu` · `duplex` · `3_4_unit` · `5plus`.

**Predictions**

| Column | Type | Notes |
|---|---|---|
| `median_days` | float | median days, application → issuance. Never null |
| `prob_issued_180d` | float32 | P(issued within 180 days) |
| `prob_issued_365d` | float32 | P(issued within 365 days) |

There is **no p90**. About 29% of housing permits are still censored, so most survival
curves plateau and never reach the 90th percentile — it is undefined for essentially every
parcel. Horizon probabilities are the honest substitute.

**Capacity breakdown** — all null (15.4%) in zones with no quantifiable residential
entitlement

| Column | Notes |
|---|---|
| `cap_base` | units permitted by base zone density |
| `cap_adu` | ADUs permitted by right |
| `cap_jadu` | JADUs permitted by right |
| `cap_sb9` | total units achievable under SB 9 — **alternative to the above, not additive** |
| `cap_total` | `max(cap_base + cap_adu + cap_jadu, cap_sb9)` |
| `delta_units` | `cap_total − existing_units`, floored at 0 |
| `cap_adu_bonus_max` | ADU Bonus ceiling — **not by-right**, 56.6% null |
| `delta_units_with_bonus` | with the bonus ceiling applied |

**Fees**

| Column | Notes |
|---|---|
| `permit_fee` | building permit only, from IB-501 Table 501A (August 2026) |
| `owes_dif` | whether Development Impact Fees apply |

**Parcel context**: `zone`, `nucleus_use_cd`, `situs_community`, `situs_zip`, `lot_sqft`,
`existing_units`, `coastal_zone`, `in_coastal_overlay`,
`coastal_deferred_certification`, `adu_eligible`, `sb9_eligible`.

`coastal_zone` values: `CST-APP` (appealable to the Commission), `N-APP-1` / `N-APP-2`
(non-appealable), `CST-PMT` (Commission retains permit authority), `DEF-CER` (deferred
certification), `CSTZB` (zone boundary), null (outside the overlay).

---

## What the UI must say

**Say "by-right capacity". Never "pre-approved."** Zoning is a standing permission — no
discretionary review is *needed*. No approval event has happened for any specific project on
any of these parcels. A planner reads "pre-approved" as a factual error and discounts
everything after it.

**`delta_units` is a screening estimate, not an entitlement determination.** It applies base
density, ADU/JADU, and SB 9 rules transcribed from the municipal code but ignores FAR,
height, setbacks, and parking — all of which bind in practice — and cannot see historic
districts, prime farmland, fire hazard severity zones, MHPA lands, or tenancy history, all of
which are SB 9 exclusions. **Real capacity is generally lower than shown.**

**`cap_adu_bonus_max` is conditional, not by-right.** Every bonus unit requires a
deed-restricted affordable ADU plus a Sustainable Development Area location. Surface it as a
separate scenario, never folded into `delta_units`.

**Inside the Coastal Overlay Zone, `cap_adu_bonus_max` is null and that is deliberate.**
Housing Action Package 1.0 and 2.0 are certified coastally, so base ADU counts and SB 9 hold
there. The 2025 amendments that created the current bonus program are *not* confirmed
certified by the Coastal Commission. Null means indeterminate — do not render it as zero.

**`coastal_deferred_certification` parcels have no certified Local Coastal Program at all.**
The Coastal Commission, not the City, is the permitting authority. By-right capacity is a
materially weaker claim on those 2,367 parcels; consider a distinct visual treatment.

**`permit_fee` is a floor, and its arithmetic is unconfirmed.** It covers plan check,
inspection, and two fixed submittal fees. It excludes Development Impact Fees, school fees,
water/sewer capacity charges, MEP and fire permits, and inclusionary housing — which together
often exceed the permit itself. Check `owes_dif`: a sub-750 sq ft ADU pays zero DIF while
every other archetype pays full community-plan-area rates, and that gap is large enough to
reorder the archetypes by true cost. The base-plus-increment reading of Table 501A could not
be verified against a primary source (the PDF is scanned images with no text layer) — confirm
with DSD before showing a dollar figure.

**The model is modest.** C-index 0.612, against 0.500 for a coin flip and 0.574 for a Cox
linear baseline. It ranks parcels better than chance, but much of what drives permit timing —
staff capacity, plan quality, revision cycles — isn't in the data. Duplex and 3–4 unit each
have under 1,000 training rows; treat those two as directional. Prefer showing a range or the
probability, not a single confident number.

**Predictions are dated.** Date features come from the precompute run
(`predictions_meta.json` → `as_of_date`). Regenerate to move the reference point.

---

## Regenerating

```bash
python scripts/acquire_parcels_full.py     # ~1.09M parcels, resumable
python scripts/train_and_save_models.py    # writes data/models/
python scripts/precompute_predictions.py   # writes predictions.parquet
python scripts/export_for_tiles.py         # writes parcels_tile_attributes.parquet
```

The permits CSV (~700 MB) downloads automatically. Budget ~45 minutes end to end. The
trained model is ~1 GB on disk and is gitignored — it is not needed to consume either output
file, only to regenerate them.
